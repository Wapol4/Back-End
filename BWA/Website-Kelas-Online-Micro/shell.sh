A.
1. Install Express Generator
    - sudo npm install -g express-generator
2. Create API Gateway
    - express --no-view api-gateway
    - cd api-gateway
    - npm install
    - npm start
3. Install dotenv untuk setting environtment
    - sudo npm install dotenv --save
4. Install axios
    - sudo npm install axios --save

B. Create Service Media
1. Create Service Media
    - express --no-view service-media
    - cd service-media
    - npm install
    - npm serve

2. Install dotenv untuk setting environtment
    - sudo npm install dotenv --save